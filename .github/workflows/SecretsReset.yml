name: Secret reset workflow

on:
  workflow_dispatch:
    # Inputs the workflow accepts.
    inputs:
      target-env:
        type: environment
        description: Select the environment
        required: true
        # default: 'demo'

jobs:
  greet:
    runs-on: ubuntu-latest
    environment: 
      name: ${{ github.event.inputs.target-env }}
      url: https://t.me/kbot_386221_bot

    steps:
    
    # Runs a single command using the runners shell
    - name: Indicate Start
      run: echo "Resetting secrets for ${{ github.event.inputs.target-env }} environment"
    
    - name: dfsg
      run: |
        echo "kubectl -n ${{ github.event.inputs.target-env }} create secret kbot --dry-run -o yaml --from-literal=$TELE_TOKEN"
